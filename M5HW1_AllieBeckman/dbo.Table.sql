CREATE TABLE [Employee]
(
	[employee ID] INT NOT NULL PRIMARY KEY DEFAULT 0, 
    [name] TEXT NOT NULL, 
    [address] TEXT NOT NULL, 
    [phone number] INT NOT NULL, 
    [position level] TEXT NOT NULL, 
    [pay rate] MONEY NOT NULL
)
